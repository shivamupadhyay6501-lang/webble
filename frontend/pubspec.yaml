name: webble
description: Free AI-powered web search assistant that answers your questions instantly
publish_to: 'none'
version: 1.0.0+1

# Replace with your info
# package: com.yourcompany.webble

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  provider: ^6.1.1
  flutter_markdown: ^0.6.18
  url_launcher: ^6.2.2
  connectivity_plus: ^5.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
